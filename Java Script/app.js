

$('#card').wScratchPad({
    size: 150,          // The size of the brush/scratch.
    bg : './Images/ticket_cover_layer.png',  // Background (image path or hex color).
    fg : './Images/ticket_cover_homepage.png',  // Foreground (image path or hex color).
    cursor      : 'pointer' // Set cursor.
  });

$('#cardAbout').wScratchPad({
    size: 150,          // The size of the brush/scratch.
    bg : './Images/ticket_cover_layer.png',  // Background (image path or hex color).
    fg : './Images/ticket_cover_aboutpage.png',  // Foreground (image path or hex color).
    cursor      : 'pointer' // Set cursor.
  });

  $('#cardBlog_1').wScratchPad({
    size: 150,          // The size of the brush/scratch.
    bg : './Images/ticket_cover_layer.png',  // Background (image path or hex color).
    fg : './Images/ticket_cover_aboutpage.png',  // Foreground (image path or hex color).
    cursor      : 'pointer' // Set cursor.
  });

  $('#cardBlog_2').wScratchPad({
    size: 150,          // The size of the brush/scratch.
    bg : './Images/ticket_cover_layer.png',  // Background (image path or hex color).
    fg : './Images/ticket_cover_aboutpage.png',  // Foreground (image path or hex color).
    cursor      : 'pointer' // Set cursor.
  });

  $('#cardBlog_3').wScratchPad({
    size: 150,          // The size of the brush/scratch.
    bg : './Images/ticket_cover_layer.png',  // Background (image path or hex color).
    fg : './Images/ticket_cover_aboutpage.png',  // Foreground (image path or hex color).
    cursor      : 'pointer' // Set cursor.
  });

  $('#cardBlog_4').wScratchPad({
    size: 150,          // The size of the brush/scratch.
    bg : './Images/ticket_cover_layer.png',  // Background (image path or hex color).
    fg : './Images/ticket_cover_aboutpage.png',  // Foreground (image path or hex color).
    cursor      : 'pointer' // Set cursor.
  });

  $('#cardBlog_5').wScratchPad({
    size: 150,          // The size of the brush/scratch.
    bg : './Images/ticket_cover_layer.png',  // Background (image path or hex color).
    fg : './Images/ticket_cover_aboutpage.png',  // Foreground (image path or hex color).
    cursor      : 'pointer' // Set cursor.
  });

  $('#cardNetart').wScratchPad({
    size: 150,          // The size of the brush/scratch.
    bg : './Images/ticket_cover_layerSilver.png',  // Background (image path or hex color).
    fg : './Images/ticket_cover_netart.png',  // Foreground (image path or hex color).
    cursor      : 'pointer' // Set cursor.
  });

  $('#cardSmall1').wScratchPad({
    size: 50,          // The size of the brush/scratch.
    bg : './Images/ticket_cover_layerSilver.png',  // Background (image path or hex color).
    fg : './Images/blog_background5.png',  // Foreground (image path or hex color).
    cursor      : 'pointer' // Set cursor.
  });

  $('#cardSmall2').wScratchPad({
    size: 50,          // The size of the brush/scratch.
    bg : './Images/ticket_cover_layerSilver.png',  // Background (image path or hex color).
    fg : './Images/blog_background4.png',  // Foreground (image path or hex color).
    cursor      : 'pointer' // Set cursor.
  });

  $('#cardSmall3').wScratchPad({
    size: 50,          // The size of the brush/scratch.
    bg : './Images/ticket_cover_layerSilver.png',  // Background (image path or hex color).
    fg : './Images/blog_background2.png',  // Foreground (image path or hex color).
    cursor      : 'pointer' // Set cursor.
  });

  $('#cardSmall4').wScratchPad({
    size: 50,          // The size of the brush/scratch.
    bg : './Images/ticket_cover_layerSilver.png',  // Background (image path or hex color).
    fg : './Images/blog_background6.png',  // Foreground (image path or hex color).
    cursor      : 'pointer' // Set cursor.
  });

  $('#cardSmall5').wScratchPad({
    size: 50,          // The size of the brush/scratch.
    bg : './Images/ticket_cover_layerSilver.png',  // Background (image path or hex color).
    fg : './Images/blog_background1.png',  // Foreground (image path or hex color).
    cursor      : 'pointer' // Set cursor.
  });

  $('#cardSmall6').wScratchPad({
    size: 50,          // The size of the brush/scratch.
    bg : './Images/ticket_cover_layerSilver.png',  // Background (image path or hex color).
    fg : './Images/blog_background3.png',  // Foreground (image path or hex color).
    cursor      : 'pointer' // Set cursor.
  });