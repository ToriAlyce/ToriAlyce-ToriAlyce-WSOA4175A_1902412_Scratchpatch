
$('#card').wScratchPad({
    size: 100,          // The size of the brush/scratch.
    bg : './Images/ticket_cover_layer.png',  // Background (image path or hex color).
    fg : './Images/ticket_cover_homepage.png',  // Foreground (image path or hex color).
    cursor      : 'pointer' // Set cursor.
  });